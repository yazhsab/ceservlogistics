# Live QA gaps and acceptance limits

Updated 19 September 2026 after deployed release `20260919-7410087-qa4`. A screen visit is not full workflow acceptance.

## Product workflow gaps

| Area | Current boundary | Next work |
| --- | --- | --- |
| COD | Collection and custody/ledger comparison pass. Transfer, reconciliation and remittance lack complete user-facing registers; some forms use raw party IDs. | Confirm list/detail contracts, provide permission-scoped identity selectors and complete handover queues; test shortages, disputes and remittance. |
| Customer portal | Linked login and read isolation exist. Self-service booking/address editing remain unavailable. | Agree intended capability and backend authorization contract before adding controls. |
| Invoice files | Invoice details, posting and correction work; invoice PDF download is unavailable. | Define authorized PDF generation/retrieval, including generation state where asynchronous. |
| Operational selectors | Several facility/reference inputs expose raw IDs. Trip arrival now has a selector. | Replace remaining raw IDs with scoped lookups where supported. |
| Store integration | Credentials/webhooks screens and contracts do not prove a configured adapter. | Select test platform/merchant, map logistics customer and exercise orders, duplicates, fulfillment and tracking. |

## Open acceptance checks

- Actual client Nigeria origin/destination postcodes and approved tariff. The isolated QA lane does not establish coverage for unspecified addresses.
- POD upload needs Chrome extension file access permission. No security setting was bypassed.
- Report generation passed (3 rows / 975 bytes). Download was clicked, but the event timed out and download-manager verification was blocked by browser policy.
- Forward intermediate hub sorting, nonzero settlements, full commission accrual, ledger reversal, populated portal scopes and external notification/webhook delivery.
- Further mobile/tablet, keyboard and accessibility acceptance. Capture 164 is desktop despite its filename; the viewport override failed.
- Account deactivation/session revocation after final acceptance. Sixteen QA users remain active intentionally.

## Closed in this pass

Portal landing, role summaries/matrix, scanner errors/focus/read-only behavior, cache/facility isolation, operational CORS, trip arrival, finance response contracts, customs valuation/customer shortcut, zero settlement, RTO labels, explicit COD custody and credit-note checker retrieval were corrected. Credit-note maker/checker enforcement remains intact; checker handoff is no longer an unresolved gap.
